# Team Revenue — knowledge log

What goes here: things this team's agents learn while working that the
next run shouldn't have to rediscover — store quirks, what a negotiation
needs before ANCHOR can close it, pricing patterns, anything about
WooCommerce's data model once it's actually reachable.

Nothing logged yet — the team hasn't run anything real (see the blocker in
`../TEAM.md`). Start a dated entry (`YYYY-MM-DD-topic.md`) the first time
one of these agents actually executes.
